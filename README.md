# my-tree-back

FastAPI backend skeleton with layered folders for `controller`, `service`, `repository`, `domain`, and `dto`.

## Run

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python run.py
```

The server port is loaded from `application.yml` and defaults to `8081`.
